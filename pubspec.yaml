name: api_cours
description: A new Flutter project.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev
version: 1.0.0+1

environment:
  sdk: '>=2.19.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  location: ^4.4.0 #pour obtenir la position de l'utilisateur
  geocoding: ^2.0.5 #pour récupérer une adresse par rapport à la latitude / longitude
  http: ^0.13.5
  shared_preferences: ^2.0.18

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^2.0.0

flutter:

  uses-material-design: true


